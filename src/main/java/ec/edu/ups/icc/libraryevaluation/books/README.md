# Módulo books

Este módulo debe ser desarrollado durante la evaluación.

Utilice únicamente la tabla `books` y `BookRepository`. No consulte otros repositorios.
La entidad y el DTO de respuesta se entregan en el enunciado HTML mediante botones de copia.
